{
  "summary": "Data class copy() bypasses companion factory validation, allowing creation of invalid Email and Money instances.",
  "findings": [
    {
      "severity": "high",
      "rule_id": "CC-K005",
      "evidence": "`Email` and `Money` data classes enforce invariants in `companion.of()` factory methods, but the auto-generated `copy()` method bypasses all validation; `PaymentService.processRefund` uses `original.copy(amount = refundAmount)` which can create a `Money` with negative amount.",
      "minimal_fix": "Add an `init` block to each data class with the same validation as the factory method so that `copy()` also enforces invariants; additionally make the primary constructor `private` to force callers through the factory.",
      "verification": "Add a test that calls `Email.of(\"valid@example.com\").copy(address = \"invalid\")` and assert it throws `IllegalArgumentException`."
    }
  ]
}

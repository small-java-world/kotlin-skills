{
  "summary": "Token generation is not deterministic and lacks clear regression boundaries.",
  "findings": [
    {
      "severity": "high",
      "rule_id": "CC-T003",
      "evidence": "issueToken directly calls LocalDateTime.now, Random.nextInt, and UUID.randomUUID.",
      "minimal_fix": "Inject `Clock`, `Random` provider, and id generator interfaces into `SessionTokenService`.",
      "verification": "Add deterministic unit tests for `SessionTokenService.issueToken()` with fixed providers and assert exact token format."
    },
    {
      "severity": "medium",
      "rule_id": "CC-T001",
      "evidence": "No tests are specified for token format boundaries and collision behavior.",
      "minimal_fix": "Add tests for `SessionTokenService.issueToken()` with userId variations, fixed time, and fixed random/id sequences.",
      "verification": "Run parameterized `issueToken` tests and confirm stable outputs under fixed providers."
    }
  ]
}
